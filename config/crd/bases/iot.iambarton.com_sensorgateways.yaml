---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: sensorgateways.iot.iambarton.com
spec:
  group: iot.iambarton.com
  names:
    kind: SensorGateway
    listKind: SensorGatewayList
    plural: sensorgateways
    singular: sensorgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SensorGateway is the Schema for the sensorgateways API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SensorGatewaySpec defines the desired state of SensorGateway.
            properties:
              brokerPort:
                type: string
              brokerURL:
                type: string
              image:
                type: string
              topic:
                type: string
            type: object
          status:
            description: SensorGatewayStatus defines the observed state of SensorGateway.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
